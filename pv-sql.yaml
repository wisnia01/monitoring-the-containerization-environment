apiVersion: v1
kind: PersistentVolume
metadata:
  name: task-pv-volume-for-sql
  labels:
    type: local
spec:
  storageClassName: local
  capacity:
    storage: 2Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/mnt/data"